// variable declaration with "let" uninitialised the variable in global memory
// same with const
// hoisting of "let" & "const" is possible
// but intialising with "unintialised" in global memory will give error


console.log(fname);
let fname = "kajal";
console.log(fname);